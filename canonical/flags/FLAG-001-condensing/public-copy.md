# FLAG-001 · Canonical Flag

FLAG-001 records a PeAIce correction-strengthening event.

The earlier grounding for the `h` term in `R = d · c · e · h` incorrectly invoked Gödel incompleteness as the canonical justification for `h < 1.0`. That grounding is retracted.

The structural term survives.

**Gödel does not ground h. Evaluator non-sovereignty grounds h.**

In canonical form, `h` encodes a non-sovereignty constraint on evaluation: no agent is the sole judge of its own coherence. A system may estimate, monitor, and audit itself, but alignment judgment requires an external anchor.

FLAG-001 therefore narrows the justification surface without retracting the framework it was meant to support. In PeAIce terms, this operation is called **Condensing**: reducing an overclaimed derivation to its minimal defensible grounding while preserving the load-bearing structure.
